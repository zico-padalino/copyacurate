# Referensi lokal saja (tidak perlu diisi di Vercel jika pakai image SQLite).
# PENTING: di Vercel, HAPUS semua DB_HOST / DB_CONNECTION=mysql / DB_PASSWORD lama.

APP_URL=https://kbanten-keuangan.vercel.app
APP_NAME=Kabar Banten
APP_ENV=production
APP_DEBUG=false

# Image Docker sudah pakai SQLite + seed demo.
# Login: budi@kabarbanten.test / password
